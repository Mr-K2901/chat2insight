def detect_topics(messages: list[dict]) -> list[str]:
    """Placeholder topic boundary; replace with the configured AI detector."""
    if not messages:
        return []
    return ["conversation"]
